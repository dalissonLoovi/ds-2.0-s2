import { Meta } from '@storybook/blocks';

<Meta title="Components/DashboardCardPrimary" />

# DashboardCardPrimary

> **Seed docs** — generated from metadata JSON. Pending React implementation (CSF3 + Autodocs).

[Open in Figma](https://www.figma.com/design/mHm12Zu9tgNmaSYnooihE5/-DS--2.0---S2?node-id=4181-493) · node `4181:493`

## AI-READY

AI-READY COMPONENT: DashboardCardPrimary — primary dashboard metric card on a dark inverse surface with donut chart accent, primary value, and two secondary metrics. Props: type=web|mobile; title; primaryValue; label1; label2; description1; description2; showInfo (BOOLEAN, default true — web-only; mobile has no info slot, intentional sparse). Anatomy web: titleRow + content (chart donut + primaryValue + metrics metricItem1|2) + info. Anatomy mobile: mainCol (chartWrap with donut + leadingIcon currency-dollar-outline + primaryValue + title) + content (metricItem1|2). Chart: chartTrack (inverse-1 web / inverse-0 mobile) + chartAccent (color/actions/secondary). Tokens: color/background-surface/inverse-4; border/radius/400; web pad spacing/200 vert + spacing/400 horiz + gap spacing/100; mobile pad/gap spacing/200; titleRow gap spacing/050; content gap spacing/300 (web) / spacing/025 (mobile); metrics gap spacing/100; metricItem gap spacing/0; chartWrap gap spacing/050; text/on-color + text/on-color-disabled; info stroke text/on-color-disabled; leadingIcon stroke text/on-color; typography body/medium/semi-bold (web title), body/small/semi-bold (mobile title), heading/h1/semi-bold (primaryValue), body/small/regular (labels), heading/h2/semi-bold (descriptions). Demo sizes: web ~257×192 HUG; mobile ~175×153. No appearance/inverse axis (dark shell is intentional via inverse-4). No growth axis (unlike DashboardCardSecondary). No hover/focus/disabled card states (intentional — static metric). Accessibility: accessible name from title + primaryValue; secondary metrics need labels; do not rely on chart color alone; info is supplementary. React mapping: DashboardCardPrimary(type, title, primaryValue, label1, label2, description1, description2, showInfo). Code Connect is not configured.

## Variants

- **type:** web | mobile

## Props

| Prop | Type / values |
| --- | --- |
| `type` | web \| mobile |
| `title` | TEXT — card title; body/medium/semi-bold (web) \| body/small/semi-bold (mobile); text/on-color-disabled |
| `primaryValue` | TEXT — hero metric; heading/h1/semi-bold + text/on-color |
| `label1` | TEXT — secondary metric label 1; body/small/regular + text/on-color-disabled |
| `label2` | TEXT — secondary metric label 2; body/small/regular + text/on-color-disabled |
| `description1` | TEXT — secondary metric value 1; heading/h2/semi-bold + text/on-color |
| `description2` | TEXT — secondary metric value 2; heading/h2/semi-bold + text/on-color |
| `showInfo` | boolean — default true; web-only (intentional sparse) |

## Rules

- **matrix:** Published 2/2 — type
- **darkInverse:** Intentional dark shell via color/background-surface/inverse-4 (not appearance/inverse axis)
- **chartAccent:** chartTrack inverse-1 (web) / inverse-0 (mobile); chartAccent color/actions/secondary
- **iconsLocal:** web info (info-circle-outline) stroke text/on-color-disabled; mobile leadingIcon (currency-dollar-outline) stroke text/on-color
- **showInfoSparse:** showInfo applies on web only — mobile has no info slot (intentional)
- **noGrowthAxis:** No growth axis (unlike DashboardCardSecondary) — static primary metric + chart
- **noInteractiveStates:** No hover/focus/disabled card states (intentional — static metric)
- **demoSizes:** web ~257×192 HUG; mobile ~175×153

## Token rules

- color/background-surface/inverse-4 (root)
- color/background-surface/inverse-0 | inverse-1 (chartTrack)
- color/actions/secondary (chartAccent)
- text/on-color | text/on-color-disabled
- border/radius/400
- spacing/400 | 300 | 200 | 100 | 050 | 025 | 0
- body/medium/semi-bold; body/small/semi-bold; body/small/regular; heading/h1/semi-bold; heading/h2/semi-bold

## Accessibility

Accessible name from title + primaryValue; secondary metrics need labels; do not rely on chart color alone; info is supplementary.

## Composition

- web: titleRow + content (chart + primaryValue + metrics) + info
- mobile: mainCol (chartWrap + primaryValue + title) + content (metricItem1|2)
- chart → chartTrack + chartAccent (+ leadingIcon on mobile)

