import { Meta } from '@storybook/blocks';

<Meta title="Components/ReferralDiscountCard" />

# ReferralDiscountCard

> **Seed docs** — generated from metadata JSON. Pending React implementation (CSF3 + Autodocs).

[Open in Figma](https://www.figma.com/design/mHm12Zu9tgNmaSYnooihE5/-DS--2.0---S2?node-id=4594-995) · node `4594:995`

## AI-READY

AI-READY COMPONENT: ReferralDiscountCard — Home/Painel referral discount meter (descontometro). Props: mode=status|simulator; title (TEXT, status heading); promoTitle (TEXT, simulator heading); description (TEXT, simulator); referralCount (TEXT); discountPercent (TEXT, status); savingsAmount + savingsCaption (TEXT, status); simulatorTitle (TEXT, simulator section title); projectedDiscount + discountAmount (TEXT, simulator). Anatomy: surface color/background-surface/2 + border/radius/400; mode=status — meter meta + 10-step track + savings + CTA; mode=simulator — absolute blur ellipses (blurPrimary/Secondary/Tertiary/Light* with LAYER_BLUR) behind content, promo header, simulator meta (simulatorTitle + referralCount), track, projection + CTA. Track: local circle-filled (completed), circle-dot-filled (current), circle-outline (pending), progressFill/progressActive rails bound to color/actions/primary; do NOT replace with ProgressBar, StepProgressIndicator, or StepperPrimary. CTA nests Button solid/md/intent=secondary (label Convidar). simulatorTitle is a non-interactive section title (body/small/semi-bold + text/primary) — not a Link. Tokens: spacing/250 pad; spacing/150 gaps; local text styles body/large/semi-bold (titles), body/small/* (meta), caption/large/regular (tick labels), heading/h2/semi-bold (savingsAmount). Rule: 10 referrals = 100% discount. Interaction: product wires onInvite — no extra Figma hover variants on the card set. Do not use as OfferProductCard, VehicleSummaryCard, Banner, or ProgressBar. Accessibility: expose progress in text (referralCount + percent); track nodes decorative when text equivalent exists; CTA keyboard-focusable; simulatorTitle is informational heading text. React mapping: ReferralDiscountCard(mode, title, promoTitle, description, referralCount, discountPercent, savingsAmount, savingsCaption, simulatorTitle, projectedDiscount, discountAmount, onInvite). Code Connect is not configured.

## Variants

- **mode:** status | simulator

## Props

| Prop | Type / values |
| --- | --- |
| `mode` | status \| simulator |
| `title` | TEXT — status heading (default Descontometro) |
| `promoTitle` | TEXT — simulator heading |
| `description` | TEXT — simulator body copy |
| `referralCount` | TEXT — e.g. 2 indicacoes |
| `discountPercent` | TEXT — status meta percent label |
| `savingsAmount` | TEXT — status savings hero |
| `savingsCaption` | TEXT — status savings caption |
| `simulatorTitle` | TEXT — simulator section title (non-interactive; not a Link) |
| `projectedDiscount` | TEXT — simulator projected percent |
| `discountAmount` | TEXT — simulator amount line |

## Status map

| Status | Icon | Background token |
| --- | --- | --- |
| status | `—` | `—` |
| simulator | `—` | `—` |

## Rules

- **composition:** surface/2 + track (10 steps) + CTA Button secondary; simulator adds blur ellipses + simulatorTitle
- **track:** circle-filled + circle-dot-filled (current) + circle-outline; progressFill → color/actions/primary — never ProgressBar/StepProgress/StepperPrimary
- **simulatorTitle:** non-interactive title text (body/small/semi-bold + text/primary) — do not use Link
- **cta:** Button solid/md/secondary label Convidar
- **noFigmaInteractionVariants:** onInvite is runtime only
- **vsOffer:** Do not use as OfferProductCard
- **vsProgress:** Do not replace track with ProgressBar / StepProgressIndicator / StepperPrimary

## Token rules

- color/background-surface/2; border/radius/400
- spacing/250 pad; spacing/150 gaps; spacing/0 where intentional
- color/actions/primary on progressFill + circle-dot-filled
- blur ellipses: color/actions/primary|secondary|tertiary + text/on-color
- body/large/semi-bold (titles); body/small/semi-bold (simulatorTitle); body/small/* (meta); caption/large/regular (ticks); heading/h2/semi-bold (savingsAmount)
- nested Button secondary owns its tokens

## Accessibility

expose progress in text; track decorative when text equivalent exists; CTA keyboard-focusable; simulatorTitle is informational heading text.

## Composition

- title | promoTitle
- meta (referralCount, discountPercent | simulatorTitle)
- track (circle-filled, progressFill, circle-dot-filled, rails, circle-outline)
- footer (savings | projection + cta Button)
- simulator blurs (absolute)

